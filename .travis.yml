language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t dockking417/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dockking417/docker-react npm run test